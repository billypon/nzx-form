nzx-form([state]="form.state", (groupChange)="form.group = $event", (submitChange)="form.submit = $event", [layout]="form.layout", (onSubmit)="form.value = $event; submit($event)", [ngClass]="form.class")
  button([hidden]="true")
